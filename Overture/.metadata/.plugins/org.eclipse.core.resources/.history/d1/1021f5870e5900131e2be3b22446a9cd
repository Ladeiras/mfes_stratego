class Player
	instance variables
		public pieces: set of Piece;
		public color: Color;
		inv color.name = [r,e,d] or
				color.name = [b,l,u,e];
		
	operations
		public getPieces: Player ==> set of Piece
		 getPieces(p) == 
		 (
		 	
		 );
		 
end Player